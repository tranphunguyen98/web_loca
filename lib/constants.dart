import 'package:flutter/material.dart';

const COLOR_ACCENT = 0xFF80ffba;
const COLOR_PRIMARY_DARK = 0xFF0AA1DD;
const COLOR_PRIMARY = 0xFF03A9F3;

const colorPrimary = Color(COLOR_PRIMARY);
const colorPrimaryDark = Color(COLOR_PRIMARY_DARK);
const colorAccent = Color(COLOR_ACCENT);
